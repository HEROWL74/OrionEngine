when:
  event: [push, pull_request]

steps:
  hello-orion:
    image: alpine
    commands:
      - echo "OrionEngine Local CI System is Online!"
      - echo "Checking build environment..."
    when:
      branch: "*"